{
  "name": "dotfiles",
  "version": "0.0.0",
  "devDependencies": {
    "husky": "^6.0.0"
  },
  "scripts": {
    "setup": "husky install && chmod +x ./scripts/permissions && ./scripts/permissions && ./install"
  }
}
